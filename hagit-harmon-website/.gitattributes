# Disable LFS for images uploaded via Netlify CMS
# This ensures CMS uploads work properly without LFS pointer issues
images/*.png !filter !diff !merge
images/*.jpg !filter !diff !merge
images/*.jpeg !filter !diff !merge
images/*.gif !filter !diff !merge
images/*.webp !filter !diff !merge
images/*.svg !filter !diff !merge

# Keep LFS for other image locations if needed
# (Uncomment and adjust paths if you have images elsewhere)
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
